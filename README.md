# The Truth

An art project that tells you the truth.

Wanna see the truth?
Well, get ready then - you may not like it.

## How to run

### With Nix

> Only tested on `aarch64-darwin` and `aarch64-linux`.

Surprisingly, `nix run .` is enough.

Alternatively, try `nix develop` and then `make run` in a dev-shell.

### Without Nix

Good luck. I don't think I can help here.

C++ is great.
